# This MVP will use the OpenAI API key from the root .env file
# No additional configuration needed - it will automatically use OPENAI_API_KEY

# Backend API Configuration (for later)
REACT_APP_API_BASE_URL=http://localhost:5000/api
